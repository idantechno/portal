# Steward — 2026-09-18

## הממצא המרכזי: תהליכי, לא קוד

`main` תקוע על commit `78a7689` (28.7.2026) — **52 יום בלי מיזוג אחד**. באותו זמן
הסטיוארד היומי המשיך לרוץ בלי הפסקה ופתח PR חדש כל יום, כל אחד עם `git checkout -b`
טרי מאותו `main` קפוא. התוצאה: **~30 PR-ים פתוחים** (#17 עד #46, ועוד #31), רובם
המכריע חוזרים על אותם שלושה תיקונים כי אף אחד מהם לא נמזג פעם אחת:

1. מוק ל-`puppeteer` ב-Jest (ESM-only שובר את `brief-renderer.spec.ts`).
2. תיקון `react-hooks/set-state-in-effect` ב-`ContactDetail.tsx`.
3. עדכוני תלויות עם CVE בתוך אותו טווח semver (`axios`, `multer`, וכו').

זה כבר תועד פעם אחת ב-PR #45 (16.9, "30 PR-ים פתוחים לא ממוזגים, main קפוא מאז 28.7")
ולא טופל מאז — עוד שני ריצות סטיוארד (#45, #46) עברו והבעיה נשארה בדיוק כמו שהייתה.

**PR #46 (אתמול, 17.9) הוא כבר superset מאומת של כל שלושת התיקונים למעלה, plus
עדכוני CVE נוספים, ועדיין ירוק ו-`mergeable_state: clean`.** אין טעם לפתוח היום
עוד PR חמישי/שלושים-ואחד שיתקן שוב את אותו דבר על אותו `main` — זה בדיוק הדפוס
שיצר את הערימה. לכן ריצת היום לא מוסיפה שינוי קוד; היא רק מדווחת.

## מה תיקנתי

כלום. קוד המאגר זהה לחלוטין לזה שנבדק אתמול (`78a7689`), ואין ממצא בטוח-וקטן
חדש שלא כבר מכוסה ומאומת ב-PR #46. פתיחת עוד branch עם אותם תיקונים הייתה
מוסיפה PR מיותר לתור, לא ערך.

## דורש החלטה

1. **לשבור את מחזור ה-PR-ים התקועים.** שלוש אופציות:
   - א׳: למזג את **#46** כמות שהוא (הכי עדכני, הכי מקיף, ירוק) ואז לסגור את
     כל שאר ה-PR-ים הישנים (#17–#45, #31) כ-redundant/superseded.
   - ב׳: לבקש ריצת סטיוארד ייעודית שתאסוף מכל ה-PR-ים התקועים רק את מה
     שלא נכלל ב-#46 (למשל אם יש תיקון ייחודי שנשכח), תאחד ל-PR נקי אחד,
     ואז לסגור את כל השאר.
   - ג׳: אם יש סיבה מכוונת ש-`main` קפוא (deploy freeze, בדיקה ידנית בעיצומה
     וכו') — לומר את זה, ואז הכי נכון להשהות את הריצה היומית של הסטיוארד
     (או להשאיר אותה על מצב "דיווח בלבד" כמו היום) עד שה-freeze מוסר, כדי
     שלא ימשיך להצטבר עוד ~PR ליום בלי תועלת.
2. **Node 20 (EOL אפריל 2026) מול puppeteer 25 שדורש Node ≥22.12** — כבר
   הועלה ב-#46, עדיין פתוח. ראו שם 3 האופציות המפורטות.
3. **CVE ב-`multer` הבא דרך `@nestjs/platform-express` (transitive)** ו-
   **`socket.io-client` תקוע על v4 עם 2 חולשות High שנפתרות רק ב-v5** —
   שניהם כבר הועלו ב-#46, עדיין ממתינים להחלטה.

## בהמתנה

- ~30 branches/PR-ים פתוחים בריפו — עומס גם על GitHub UI וגם על דקות CI.
- TODO קיים ב-`widget.controller.ts` בנוגע ל-`widgetAllowedOrigins` (הועלה
  בעבר; PR #41 טען שזה כבר ממומש ב-`WidgetService` — כדאי לוודא ולסגור סופית).
- אין test runner מוגדר לפרונט (ידוע, מתועד ב-CLAUDE.md).
- אזהרת peer dependency לא חוסמת: `i18next`/`react-i18next` מצפים ל-TS `^5`,
  מותקן `6.0.3`.
- 3 חולשות low/moderate transitive נוספות דרך `@modelcontextprotocol/sdk`.

## וידוא

אין שינוי קוד היום, אז אין build/lint/test חדש להריץ. הבדיקות הרלוונטיות
(build+lint+test על backend/frontend) בוצעו ואומתו על אותו commit `78a7689`
בריצות הקודמות (#45, #46) — לא הרצתי אותן שוב כי שום קובץ לא זז.
