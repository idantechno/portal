# דוח סטיוארד — 2026-09-16

## הכי חשוב עכשיו

לא בוצע שינוי קוד היום. הממצא המרכזי הוא לא בקוד אלא בתהליך: `main` לא זז מאז **28.7.2026** (commit `78a7689`, אותו יום שבו הוקם הסטיוארד עצמו) — כמעט שבעה שבועות. מאז נפתחו **30 PR-ים** של הסטיוארד (`#15` עד `#44`, כמעט כל יום ברצף) ואף אחד מהם לא מוזג. האוטומציה עובדת כמתוכנן; שום ממצא שלה לא מגיע בפועל למוצר.

## מה נמצא

- `origin/main` נמצא בדיוק על אותו commit כמו ביום שבו הסטיוארד הוקם.
- 30 PR-ים פתוחים תחת `steward/YYYY-MM-DD`, כולם עם `mergeable_state: clean` מול אותו base — אין התנגשויות טכניות שחוסמות מיזוג.
- **PR #31** (31.8) זיהה ותיקן תקלה אמיתית: `pnpm test` בבקאנד היה נכשל לגמרי (0 טסטים רצים) כי `puppeteer` מפרסם ESM טהור, וזה שבר את ה-parse של `website-extractor.service.ts` דרך `brief-renderer.spec.ts`.
- **PR #44** (אתמול) חוזר על אותו תיקון (stub ל-puppeteer דרך `moduleNameMapper`) ומוסיף: תיקון lint שבור ב-`ContactDetail.tsx` (`react-hooks/set-state-in-effect`), עדכון `axios` (סוגר כמה CVE בדרג בינוני-גבוה, כולל SSRF דרך redirect), ועדכון `multer` (סוגר עקיפת מגבלת גודל קובץ). מאומת במלואו — build+lint+test ירוקים בבקאנד ובפרונט כאחד.
- בדקתי בפועל (`git diff --stat` מול ה-base המשותף) ש-PR #44 מכיל כל השינויים שהיו ב-#31, ועוד — כלומר #44 הוא כבר superset מאומת של לפחות אחד מהתיקונים החשובים בערימה. סביר שהמצב דומה גם ביחס לחלק מ-#15–#43, אבל זה לא נבדק שורה-שורה לכל 30 ה-PR-ים.
- בגלל זה לא נפתח כאן PR מקביל עם אותם ממצאים — main זהה לחלוטין למה שהיה כשנבדק אתמול, ואין תועלת בלרוץ שוב על אותו קוד.

## דורש החלטה

1. **למזג את #44 ולסגור את השאר כ"הוחלף"** — הכי מהיר: PR אחד ומאומת מביא ל-main את התיקונים הקריטיים שכבר קיימים בערימה (טסטים שבורים, CVE-ים, lint). סיכון: לא נסרק כל אחד מ-15–43 שורה-שורה, ותיאורטית יכול להיות בהם ממצא ייחודי שלא נכנס ל-44.
2. **מעבר ידני על כל ה-30 PR-ים לפני סגירה** — בטוח יותר מפני אובדן ממצא ייחודי, אבל יקר בזמן ולא פרופורציונלי לרוב הסיכוי שהם חופפים.
3. **הגדרת כלל לסטיוארד עצמו**: כשנפתח PR חדש שמכיל את כל הקבצים של PR קודם שלו שעדיין פתוח, לסגור אוטומטית את הקודם כ"הוחלף" עם קישור לחדש — מונע הצטברות עתידית, אבל דורש שהסטיוארד ישווה מול PR-ים פתוחים קודמים בתחילת כל ריצה (לא רק מול `main`).

## בהמתנה

- לא בוצע סבב build/lint/test מלא היום — מצב הקוד זהה לחלוטין לאתמול (main לא זז), והתוצאות המאומתות של #44 עדיין תקפות במלואן.
- `widget.controller.ts:20` — TODO ידוע (CORS/Origin לפי `Business.widgetAllowedOrigins`), עדיין לא טופל.
- כדאי לשקול `pnpm audit --prod` כבדיקת CI קבועה (הועלה גם ב-#44) כדי לתפוס פערי CVE לפני שהם מצטברים.
- הצטברות 30 PR-ים פתוחים היא איתות בפני עצמה: כדאי מנגנון תזכורת/סיכום שבועי ל-PR-ים שפתוחים מעל שבוע.

## אימות

לא בוצע שינוי קוד בריצה הזו, ולכן לא הורץ build/lint מחדש. הבדיקה שבוצעה בפועל: `git diff --stat` בין ה-base המשותף (`78a7689`) לבין `origin/steward/2026-09-15`, שאישר ש-PR #44 מכיל את כל הקבצים שתוקנו ב-PR #31 (`backend/package.json`, `backend/pnpm-lock.yaml`, קובץ ה-stub, ו-`tsconfig.build.json`), בנוסף לתיקוני lint/axios/multer. תוצאות ה-build/lint/test המלאות של #44 (backend: build ✅, lint ✅, 6/6 סוויטות ו-27/27 טסטים ✅; frontend: build ✅, lint ✅) מדווחות בגוף אותו PR ועדיין תקפות כי main לא השתנה מאז.
