export enum ContactStatus {
  /** Prospective customer — showed interest but hasn't bought yet. */
  Lead = 'lead',
  /** Paying customer — treat with familiarity and priority. */
  Customer = 'customer',
}
