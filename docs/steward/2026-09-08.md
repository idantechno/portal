# Steward — 2026-09-08

## הכי חשוב עכשיו: תור ה-PRs לא זז

יש **20 pull request פתוחים** של ה-steward ב-GitHub, `steward/2026-08-17` עד `steward/2026-09-07`
(PR #19–#38), אף אחד מהם לא מוזג. `main` לא זז מאז 2026-07-28 (commit `78a7689`).

המשמעות: כל ריצה יומית מאבחנת מחדש **אותם שני תיקונים בדיוק**, כי ה-`main` שהיא
עובדת מולו לא השתנה:

1. `puppeteer@25.2.0` הוא ESM טהור ושובר את `pnpm test` בבקאנד
   (`brief-renderer.spec.ts`, דרך `website-extractor.service.ts`) — תוקן כבר
   ב-PR #31 (31.8) וב-PR #38 (7.9), ותוקן שוב היום (ראו למטה).
2. `ContactDetail.tsx` קרא ל-`setState` בתוך `useEffect` לצורך seed של הטופס,
   מה שמפיל את `react-hooks/set-state-in-effect` ב-lint של הפרונטאנד — תוקן
   כבר ב-PR #38, ותוקן שוב היום.

זה בזבוז: 22 ריצות (כולל היום) הפיקו את אותו תיקון פעמיים-שלוש, ואף אחת לא
נכנסה ל-`main`. ה-PR הזה (#39 ואילך) הוא עוד עותק לערימה אלא אם ייסקר.

**המלצה:** לבדוק/למזג PR אחד עם שני התיקונים (למשל את זה, שמכיל את שניהם
ומאומת מלמטה) ואז לסגור את שאר ה-PRs הכפולים (#19–#38) בלי מיזוג — הם כולם
נגזרים מאותו `main` ישן ולא מוסיפים מידע מעבר למה שכאן.

## מה תיקנתי (מאומת)

- **backend** — `package.json`: הוספת `moduleNameMapper` הממפה `puppeteer`
  ל-stub חדש ב-`test/mocks/puppeteer.ts`, כדי ש-Jest לא ינסה לפרסר את ה-ESM
  entry point האמיתי. אף טסט לא מפעיל דפדפן אמיתי, אז זה לא-הרסני.
- **frontend** — `ContactDetail.tsx`: הוחלף ה-seed של שדות הטופס מ-`useEffect`
  לדפוס "עדכון state בזמן רינדור" (עוקב אחרי `contact.id` שכבר הוזרע), התואם
  להמלצת ה-docs של React לתרחיש הזה — אותה התנהגות, בלי את ה-lint error.

אימות:
- `backend`: `pnpm install --frozen-lockfile` ✅ · `pnpm build` ✅ · `pnpm lint` ✅ · `pnpm test` — 6/6 סוויטות, 27/27 טסטים ✅
- `frontend`: `pnpm install --frozen-lockfile` ✅ · `pnpm build` ✅ · `pnpm lint` ✅

## דורש החלטה

- **תור ה-PRs** (למעלה) — הפריט המרכזי של היום. אין כאן שינוי קוד לבחור בו,
  אלא תהליך: לבחור PR אחד למיזוג ולסגור את השאר, או להגדיר שה-steward יבדוק
  קודם PRs פתוחים לפני שהוא פותח חדש (כדי לא לייצר כפילויות בעתיד).

## בהמתנה

- `widget.controller.ts` עדיין עם `TODO(prod)`: אכיפת
  `Business.widgetAllowedOrigins` על CORS/Origin לא ממומשת (נצפה גם ב-PR #38).
- Puppeteer מיושן (25.2.0 מותקן, יש 25.10.0) — לא עודכן; גרסה חדשה יותר שברה
  את אותה סוויטת טסטים בניסיון קודם (ראו PR #38), וה-stub החדש לא נבדק מולה.
- כמה תלויות backend מיושנות ברמת patch/minor — לא נגעתי כדי לשמור על ה-PR ממוקד.
