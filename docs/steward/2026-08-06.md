# Steward — 2026-08-06

## מצב

`main` (`78a7689`) עדיין באותו commit כמו ב-31/07. CI על `main` אדום ברציפות **מ-26/27.07, כלומר 10 ימים**, כולל הריצה האחרונה על ה-tip הנוכחי (28/07, נכשלה). הסיבה הבסיסית ידועה ומתועדת כבר בכמה סבבי Steward: puppeteer (ESM-only) ש-Jest לא מצליח לפרסר דרך `website-extractor.service.ts` → `brief-renderer.spec.ts`, בתוספת כשל lint ב-`ContactDetail.tsx` (`react-hooks/set-state-in-effect`).

## הממצא המרכזי של הסבב הזה

יש **חמישה PR-ים פתוחים ולא ממוזגים** מסבבי Steward קודמים, כולם נשענים על אותו `main` (78a7689) ואף אחד מהם לא זז מאז שנפתח:

| PR | תאריך | סטטוס CI | תוכן ייחודי |
|---|---|---|---|
| [#4](https://github.com/idantechno/portal/pull/4) | 31/07 | ירוק (בזמנו) | jest mock ל-puppeteer, תיקון TODO ב-widget.controller.ts, multer 2.2.0 |
| [#5](https://github.com/idantechno/portal/pull/5) | 01/08 | אין שינוי קוד | רק דיווח מצב — לא מוסיף כלום מעבר ל-#4 |
| [#6](https://github.com/idantechno/portal/pull/6) | 02/08 | ירוק (בזמנו) | פיצול website-extraction.types.ts, **הסרת קוד מת** (`findById` ב-customer-contacts.service.ts), **תיקון אייקון RTL** ב-Automations.tsx (חץ ליטרלי ← chevron-end), multer+typeorm+axios+react-router-dom |
| [#7](https://github.com/idantechno/portal/pull/7) | 03/08 | ירוק (בזמנו) | jest moduleNameMapper mock — גישה חלופית לאותה בעיה, בלי תוספות |
| [#8](https://github.com/idantechno/portal/pull/8) | 04/08 | ירוק (בזמנו) | גישה שלישית (dynamic import), **socket.io-parser bump**, **mcp-sdk bump**, react-router-dom+axios |
| [**#9**](https://github.com/idantechno/portal/pull/9) | 05/08 | **ירוק עכשיו, `mergeable_state: clean` מול `main` הנוכחי** | פיצול website-extraction.types.ts (כמו #6), react-router-dom, ContactDetail |

כל אחד מהם פותר את בעיית ה-CI האדום בגישה מעט שונה, אבל אף אחד אינו superset מלא של האחרים — יש עבודה אמיתית שלא שוכפלה (הסרת קוד מת + תיקון RTL ב-#6, socket.io-parser+mcp-sdk ב-#8). זו בדיוק התבנית שסבבי Steward קודמים (PR #5, #8 עצמם) כבר הזהירו מפניה: כל עוד אף PR לא ממוזג, כל סבב חדש רק מוסיף ענף מקביל שמגלה את אותה תקלה מחדש.

**לא פתחתי PR שישי שמתקן את אותו הדבר שוב.** זה היה רק מגדיל את הבלגן.

## המלצה

1. למזג את **#9** עכשיו — ירוק, נקי, פותר את אדום ה-CI מיידית.
2. אחרי המיזוג, לעבור על מה שנשאר ייחודי ב-#6 (קוד מת + אייקון RTL) וב-#8 (socket.io-parser + mcp-sdk) ולהחליט אם להעביר אותם ל-PR קטן נפרד או לזרוק.
3. לסגור את #4, #5, #7 — מוחלפים לגמרי ע"י #9 ואינם תורמים דבר ייחודי.

לא בוצע שינוי קוד בסבב הזה — הכל כבר קיים ומאומת ב-PR-ים הפתוחים; החוסם הוא מיזוג, לא קוד.
