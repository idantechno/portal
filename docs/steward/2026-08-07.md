# Steward — 2026-08-07

## מצב

`main` (`78a7689`) לא זז מ-28/07 — יומיים אחרי שהסבב הקודם (PR #10, 06/08) דיווח בדיוק את אותו ממצא. CI על `main` עדיין אדום, עכשיו **13 יום ברציפות** (מ-25/26.07), אותה תקלה ידועה בשני jobs:

- **backend** — `pnpm run test` נכשל (`brief-renderer.spec.ts` מושך `puppeteer`, חבילת ESM-only, ש-Jest לא מצליח לפרסר דרך `website-extractor.service.ts`).
- **frontend** — `pnpm run lint` נכשל (`ContactDetail.tsx`, קריאת `setState` בתוך `useEffect` בזמן טעינת כרטיס לקוח — נגד `eslint-plugin-react-hooks`).

אימתתי מחדש (ריצת CI #44 על ה-tip הנוכחי של `main`): שני הכשלים זהים למה שתועד ב-05/08 וב-06/08 — שום דבר חדש לא נשבר.

## הממצא המרכזי של הסבב הזה

עדיין **שישה PR-ים פתוחים ולא ממוזגים** (#4–#9, ועכשיו גם #10 עצמו), כולם נשענים על אותו `main`. שום דבר לא זז מאז הדיווח האתמול:

- **[#9](https://github.com/idantechno/portal/pull/9)** — עדיין `mergeable_state: clean` מול ה-`main` הנוכחי, עדיין הפתרון הכי מלא והכי ירוק. זה הפריט היחיד שבאמת חוסם קדימה.
- #4, #5, #7 — משוכפלים לגמרי ע"י #9, לא תורמים כלום ייחודי.
- #6 — הסרת קוד מת (`findById` לא בשימוש ב-`customer-contacts.service.ts`) + תיקון אייקון RTL ב-`Automations.tsx` (חץ ליטרלי → `chevron-end`). ייחודי, לא ב-#9.
- #8 — עדכוני `socket.io-parser` + `@modelcontextprotocol/sdk`. ייחודי, לא ב-#9.

**בדקתי היום אפשרות לתיקון בטוח נוסף ולא מצאתי אחד אמיתי**: ניסיתי לעדכן את `@modelcontextprotocol/sdk` בתוך אותו טווח semver (`^1.29.0` → `1.30.0`, כמו ש-#8/#9 עשו לחבילות אחרות) — זה לא פתר אף אחת מ-22 חולשות ה-audit (כולן transitive דרך `hono`/`express`/`body-parser` בגרסה פנימית שלא זזה ב-1.30.0). ביטלתי את השינוי — עדכון בלי תועלת אבטחתית אמיתית הוא רק רעש בלוקפייל.

**לא פתחתי PR שביעי שמתקן את אותה תקלה שוב.** החוסם עדיין מיזוג, לא קוד.

## המלצה (זהה לסבב הקודם — עדיין לא בוצעה)

1. למזג את **#9** — ירוק, נקי, פותר את אדום ה-CI מיידית.
2. אחרי המיזוג: PR קטן שמלקט את מה שייחודי ב-#6 (קוד מת + RTL) וב-#8 (socket.io-parser + mcp-sdk), ואז לסגור את #4, #5, #6, #7, #8 (ו-#9, #10 עצמם אחרי המיזוג).
3. חולשת `react-router` v8 (RSC Mode CSRF, high) ו-22 ממצאי audit ב-`@modelcontextprotocol/sdk` (transitive) — עדיין ממתינים להחלטה, ללא שינוי מהסבב הקודם.

לא בוצע שינוי קוד בסבב הזה — הכל כבר קיים ומאומת ב-PR-ים הפתוחים; החוסם הוא מיזוג, לא קוד.
